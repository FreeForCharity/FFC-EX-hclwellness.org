// Makes @testing-library/jest-dom's matcher type augmentations (toBeInTheDocument,
// toHaveAttribute, ...) visible to the compiler now that tsconfig pins "types".
import '@testing-library/jest-dom'
